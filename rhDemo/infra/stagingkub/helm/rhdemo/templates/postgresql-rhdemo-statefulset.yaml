{{- if .Values.postgresql-rhdemo.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql-rhdemo
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "postgresql-rhdemo.labels" . | nindent 4 }}
    app: postgresql-rhdemo
spec:
  serviceName: postgresql-rhdemo
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-rhdemo
  template:
    metadata:
      labels:
        app: postgresql-rhdemo
        {{- include "postgresql-rhdemo.labels" . | nindent 8 }}
    spec:
      containers:
      - name: postgresql
        image: "{{ .Values.postgresql-rhdemo.image.repository }}:{{ .Values.postgresql-rhdemo.image.tag }}"
        imagePullPolicy: {{ .Values.postgresql-rhdemo.image.pullPolicy }}
        ports:
        - name: postgresql
          containerPort: {{ .Values.postgresql-rhdemo.service.port }}
          protocol: TCP
        env:
        - name: POSTGRES_DB
          value: {{ .Values.postgresql-rhdemo.database.name }}
        - name: POSTGRES_USER
          value: {{ .Values.postgresql-rhdemo.database.user }}
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.postgresql-rhdemo.database.passwordSecret.name }}
              key: {{ .Values.postgresql-rhdemo.database.passwordSecret.key }}
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgresql-data
          mountPath: /var/lib/postgresql/data
        - name: init-scripts
          mountPath: /docker-entrypoint-initdb.d
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U {{ .Values.postgresql-rhdemo.database.user }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U {{ .Values.postgresql-rhdemo.database.user }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        resources:
          {{- toYaml .Values.postgresql-rhdemo.resources | nindent 10 }}
      volumes:
      - name: init-scripts
        configMap:
          name: postgresql-rhdemo-init
  volumeClaimTemplates:
  - metadata:
      name: postgresql-data
      labels:
        app: postgresql-rhdemo
    spec:
      accessModes:
      - {{ .Values.postgresql-rhdemo.persistence.accessMode }}
      {{- if .Values.postgresql-rhdemo.persistence.storageClass }}
      storageClassName: {{ .Values.postgresql-rhdemo.persistence.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.postgresql-rhdemo.persistence.size }}
{{- end }}
