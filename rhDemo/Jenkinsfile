pipeline {
    agent any

    options {
        // Timeout global : échoue si le pipeline dépasse 2 heures
        timeout(time: 2, unit: 'HOURS')

        // Ajoute des timestamps dans les logs console
        timestamps()

        // Conservation des builds (garde les 30 derniers)
        buildDiscarder(logRotator(numToKeepStr: '30', artifactNumToKeepStr: '10'))
    }

    tools {
        jdk 'JDK21'  // Nom configuré dans Jenkins Global Tool Configuration
        maven 'Maven3' // Nom configuré dans Jenkins Global Tool Configuration
    }

    environment {
        // Configuration du pipeline
        APP_NAME = 'rhdemo-api'
        // La version est lue dynamiquement depuis pom.xml (voir stage 'Lecture Version')
        APP_VERSION = ''
        JAR_FILE = ''

        // Configuration notifications email
        // À configurer : remplacer par votre email ou utiliser Jenkins Credentials
        //NOTIFICATION_EMAIL = 'moi@monserveur.com'

        // Configuration Docker
        DOCKER_IMAGE_NAME = "rhdemo-api"
        DOCKER_IMAGE_TAG = ''
        DOCKER_REGISTRY = "" // Laisser vide pour Docker Hub ou spécifier registry privé

        // Versions des images Docker de base
        NGINX_IMAGE = "nginx:1.27-alpine"
        POSTGRES_IMAGE = "postgres:16-alpine"
        KEYCLOAK_IMAGE = "quay.io/keycloak/keycloak:26.4.2"

        // Configuration des tests
        TEST_PORT = '9000'
        TEST_PROJECT_PATH = 'rhDemoAPITestIHM'

        // Fichiers de secrets SOPS
        SECRETS_FILE = 'rhDemo/secrets/secrets-staging.yml'
        SECRETS_DECRYPTED = 'rhDemo/secrets/secrets-decrypted.yml'
        SECRETS_ENV_VARS = 'rhDemo/secrets/env-vars.sh'
        SECRETS_RHDEMO = 'rhDemo/secrets/secrets-rhdemo.yml'

        // Configuration environnement staging (Docker Compose)
        STAGING_INFRA_PATH = 'rhDemo/infra/staging'
        KEYCLOAK_INIT_PATH = 'rhDemoInitKeycloak'
        TEST_DOMAIN = 'rhdemo.staging.local'
        KEYCLOAK_DOMAIN = 'keycloak.staging.local'
        COMPOSE_PROJECT_NAME = "rhdemo-staging-${env.BUILD_NUMBER}"
        RHDEMO_IMAGE = "${DOCKER_IMAGE_NAME}:build-${env.BUILD_NUMBER}"

        // Noms des conteneurs Docker (centralisés pour faciliter maintenance)
        CONTAINER_NGINX = 'rhdemo-staging-nginx'
        CONTAINER_APP = 'rhdemo-staging-app'
        CONTAINER_KEYCLOAK = 'keycloak-staging'
        CONTAINER_KEYCLOAK_DB = 'keycloak-staging-db'
        CONTAINER_DB = 'rhdemo-staging-db'
        CONTAINER_ZAP = 'rhdemo-jenkins-zap'

        // Noms des réseaux Docker
        NETWORK_STAGING = 'rhdemo-staging-network'
        NETWORK_JENKINS = 'rhdemo-jenkins-network'
    }
    
    parameters {
        choice(name: 'DEPLOY_ENV', choices: ['staging', 'production', 'none'], description: 'none=build+tests unitaires/intégration+SonarQube (sans Docker/Selenium), staging/production=pipeline complet')
        booleanParam(name: 'RUN_SELENIUM_TESTS', defaultValue: true, description: 'Exécuter les tests Selenium (ignoré si DEPLOY_ENV=none)')
        booleanParam(name: 'RUN_SONAR', defaultValue: true, description: 'Exécuter l\'analyse SonarQube')
        booleanParam(name: 'SKIP_MANUAL_APPROVAL', defaultValue: false, description: 'Ignorer l\'approbation manuelle pour la production')
    }
    
    stages {
        
        // ════════════════════════════════════════════════════════════════
        // PHASE 1 : PRÉPARATION
        // ════════════════════════════════════════════════════════════════
        
        stage('🛠️ Checkout') {
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 1 : PRÉPARATION'
                    echo '═══════════════════════════════════════════════════════'
                }
                checkout scm
                sh 'git log -1 --pretty=format:"%h - %an : %s"'

                script {
                    echo '▶ Vérification du projet rhDemoInitKeycloak...'
                    // rhDemoInitKeycloak est dans le MÊME repo que rhDemo
                    // Le checkout SCM récupère en principe tout le workspace
                }

                sh '''
                    if [ -d "rhDemoInitKeycloak" ]; then
                        echo "✅ rhDemoInitKeycloak trouvé dans le workspace"
                        ls -la rhDemoInitKeycloak
                    else
                        echo "❌ rhDemoInitKeycloak introuvable"
                        echo "Structure du workspace:"
                        ls -la
                        exit 1
                    fi
                '''
            }
        }

        stage('🔢 Lecture Version Maven') {
            steps {
                script {
                    echo '▶ Lecture de la version depuis pom.xml...'

                    // Lire la version depuis le pom.xml avec Maven
                    // Rediriger stderr vers /dev/null pour éviter pollution de stdout
                    def versionOutput = sh(
                        script: 'cd rhDemo && ./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout 2>/dev/null',
                        returnStdout: true
                    ).trim()

                    echo "🔍 Debug - Version brute lue: '${versionOutput}'"
                    echo "🔍 Debug - Longueur: ${versionOutput.length()}"

                    env.APP_VERSION = versionOutput

                    // Vérifier que la lecture a réussi
                    if (!env.APP_VERSION || env.APP_VERSION == '' || env.APP_VERSION == 'null') {
                        error("❌ Impossible de lire la version depuis pom.xml. Version détectée: '${env.APP_VERSION}'")
                    }

                    // Construire le nom du fichier JAR avec la version lue
                    env.JAR_FILE = "rhDemo/target/rhdemoAPI-${env.APP_VERSION}.jar"

                    // Mettre à jour les variables Docker
                    env.DOCKER_IMAGE_TAG = env.APP_VERSION

                    echo "✅ Version Maven détectée: ${env.APP_VERSION}"
                    echo "   JAR attendu: ${env.JAR_FILE}"
                    echo "   Docker tag: ${env.DOCKER_IMAGE_TAG}"
                }
            }
        }

        stage('🔑 Initialisation Variables Dynamiques') {
            steps {
                script {
                    echo '▶ Génération de la clé API OWASP ZAP...'

                    // Générer une clé API aléatoire pour ZAP ou récupérer depuis secrets
                    def zapApiKey = sh(
                        script: '''
                            # Essayer de charger depuis secrets si disponible
                            if [ -f "rhDemo/secrets/env-vars.sh" ]; then
                                . rhDemo/secrets/env-vars.sh
                                if [ -n "$ZAP_API_KEY" ]; then
                                    echo "$ZAP_API_KEY"
                                    exit 0
                                fi
                            fi
                            # Sinon générer une nouvelle clé
                            cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1
                        ''',
                        returnStdout: true
                    ).trim()

                    // Stocker comme variable d'environnement globale du pipeline
                    env.ZAP_API_KEY = zapApiKey
                    echo "✅ Clé API ZAP initialisée: ${zapApiKey.take(8)}..."
                }
            }
        }

        stage('🔓 Déchiffrement Secrets SOPS') {
            steps {
                script {
                    echo '▶ Vérification SOPS et YQ...'
                }
                sh '''
                    sops --version
                    yq --version
                '''
                
                script {
                    echo '▶ Déchiffrement du fichier de secrets...'
                }
                
                // Utiliser le credential sops-age-key de manière sécurisée
                withCredentials([file(credentialsId: 'sops-age-key', variable: 'SOPS_AGE_KEY_FILE')]) {
                    sh '''
                        # Créer le répertoire secrets s'il n'existe pas
                        mkdir -p rhDemo/secrets
                        
                        # Déchiffrer le fichier de secrets
                        echo "Déchiffrement de ${SECRETS_FILE}..."
                        sops -d ${SECRETS_FILE} > ${SECRETS_DECRYPTED}
                        
                        # Vérifier que le fichier a été déchiffré
                        if [ ! -f ${SECRETS_DECRYPTED} ]; then
                            echo "❌ Échec du déchiffrement"
                            exit 1
                        fi
                        
                        echo "✅ Secrets déchiffrés avec succès"
                        
                        # Extraire les valeurs pour les variables d'environnement
                        # SÉCURITÉ: Désactiver l'écho des commandes pour ne pas exposer les secrets dans les logs
                        set +x

                        # Exporter les secrets comme variables d'environnement pour les stages suivants
                        # Utilisation de substitution de variables sans affichage dans stdout
                        {
                            # PROTECTION: Désactiver l'écho pour éviter l'exposition des secrets lors du sourcing
                            echo "# SÉCURITÉ: Ce fichier contient des secrets - désactive automatiquement le tracing bash"
                            echo "# Désactiver le tracing pour ne pas logger les secrets"
                            echo "{ set +x; } 2>/dev/null"
                            echo ""

                            echo "export RHDEMO_DATASOURCE_PASSWORD_PG=$(yq eval '.rhdemo.datasource.password.pg' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_DATASOURCE_PASSWORD_H2=$(yq eval '.rhdemo.datasource.password.h2' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET=$(yq eval '.rhdemo.client.registration.keycloak.client.secret' ${SECRETS_DECRYPTED})"

                            # Mots de passe Keycloak pour environnement staging
                            echo "export KEYCLOAK_DB_PASSWORD=$(yq eval '.keycloak.db.password' ${SECRETS_DECRYPTED})"
                            echo "export KEYCLOAK_ADMIN_PASSWORD=$(yq eval '.keycloak.admin.password' ${SECRETS_DECRYPTED})"
                            echo "export KEYCLOAK_ADMIN_USER=$(yq eval '.keycloak.admin.user // "admin"' ${SECRETS_DECRYPTED})"

                            # Utilisateurs et mots de passe de test (chiffrés dans SOPS)
                            echo "export RHDEMO_TEST_ID_USER_ADMIN=$(yq eval '.rhdemo.test.iduseradmin' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_TEST_PWD_USER_ADMIN=$(yq eval '.rhdemo.test.pwduseradmin' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_TEST_ID_USER_MAJ=$(yq eval '.rhdemo.test.idusermaj' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_TEST_PWD_USER_MAJ=$(yq eval '.rhdemo.test.pwdusermaj' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_TEST_ID_USER_CONSULT=$(yq eval '.rhdemo.test.iduserconsult' ${SECRETS_DECRYPTED})"
                            echo "export RHDEMO_TEST_PWD_USER_CONSULT=$(yq eval '.rhdemo.test.pwduserconsult' ${SECRETS_DECRYPTED})"

                            # URLs des serveurs si elles existent
                            if yq eval '.rhdemo.servers.staging' ${SECRETS_DECRYPTED} > /dev/null 2>&1; then
                                echo "export STAGING_SERVER=$(yq eval '.rhdemo.servers.staging' ${SECRETS_DECRYPTED})"
                            fi

                            if yq eval '.rhdemo.servers.production' ${SECRETS_DECRYPTED} > /dev/null 2>&1; then
                                echo "export PROD_SERVER=$(yq eval '.rhdemo.servers.production' ${SECRETS_DECRYPTED})"
                            fi
                        } > rhDemo/secrets/env-vars.sh 2>&1

                        # Réactiver l'écho des commandes
                        set -x

                        # Afficher (sans montrer les valeurs)
                        echo "✅ Variables d'environnement préparées"
                    '''
                }
            }
            post {
                always {
                    script {
                        // Supprimer le fichier déchiffré pour la sécurité
                        sh 'rm -f ${SECRETS_DECRYPTED} || true'
                    }
                }
            }
        }

        stage('🔐 Extraction secrets rhDemo') {
            steps {
                script {
                    echo '▶ Création du fichier secrets-rhdemo.yml pour rhDemo (uniquement les secrets nécessaires)...'
                }

                sh '''
                    # SÉCURITÉ: Désactiver l'écho des commandes
                    set +x

                    # Source les secrets
                    . rhDemo/secrets/env-vars.sh

                    # Créer un fichier secrets-rhdemo.yml contenant UNIQUEMENT les secrets rhDemo
                    # Ce fichier sera monté dans le container Docker de rhDemo
                    cat > rhDemo/secrets/secrets-rhdemo.yml <<EOF

rhdemo:
  datasource:
    password:
      pg: ${RHDEMO_DATASOURCE_PASSWORD_PG}
      h2: ${RHDEMO_DATASOURCE_PASSWORD_H2}
  client:
    registration:
      keycloak:
        client:
          secret: ${RHDEMO_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET}
EOF

                    # Réactiver l'écho
                    set -x

                    echo "✅ Fichier rhDemo/secrets/secrets-rhdemo.yml créé (secrets limités à rhDemo uniquement)"
                '''
            }
        }

        stage('🔧 Configuration rhDemoInitKeycloak') {
            steps {
                script {
                    echo '▶ Génération de application-staging.yml pour rhDemoInitKeycloak...'
                    echo '   (Sera utilisé directement via mvnw spring-boot:run, sans packaging JAR)'
                }
                
                sh '''
                    # SÉCURITÉ: Désactiver l'écho des commandes pour ne pas exposer les secrets
                    set +x

                    # Source les secrets déchiffrés
                    . rhDemo/secrets/env-vars.sh

                    # Générer application-staging.yml (sera utilisé directement par Maven)
                    # NOTE: set +x reste actif pendant la génération pour ne pas logger les secrets
                    CONFIG_FILE="rhDemoInitKeycloak/src/main/resources/application-staging.yml"

                    # IMPORTANT: Sans guillemets sur YMLEOF pour que bash substitue les variables
                    cat > "${CONFIG_FILE}" << YMLEOF
# ========================================
# Configuration Keycloak Server - STAGING
# Générée automatiquement par Jenkins CI/CD
# ========================================
keycloak:
  # URL interne HTTP pour rhDemoInitKeycloak (exécuté dans le réseau Docker)
  # Pas besoin de HTTPS car communication interne sécurisée par le réseau isolé
  server-url: http://keycloak-staging:8080
  
  admin:
    realm: master
    username: ${KEYCLOAK_ADMIN_USER}
    password: ${KEYCLOAK_ADMIN_PASSWORD}
  
  # ========================================
  # Configuration du Realm RHDemo
  # ========================================
  realm:
    name: RHDemo
    display-name: RHDemo Application
    enabled: true
    registration-allowed: false
    registration-email-as-username: false
    reset-password-allowed: true
    edit-username-allowed: false
    login-with-email-allowed: true
    duplicate-emails-allowed: false
    remember-me: true
    sso-session-idle-timeout: 1800
    sso-session-max-lifespan: 36000
    access-token-lifespan: 300
  
  # ========================================
  # Configuration du Client RHDemo
  # ========================================
  client:
    client-id: RHDemo
    name: RHDemo
    secret: ${RHDEMO_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET}
    root-url: https://${TEST_DOMAIN}/
    base-url: ''
    admin-url: ''
    redirect-uris:
      - https://${TEST_DOMAIN}/*
      - http://localhost:9000/*
      - https://rhdemo.staging.local/*
      - https://keycloak.staging.local/*
    web-origins:
      - https://${TEST_DOMAIN}
      - http://localhost:9000
      - https://rhdemo.staging.local
      - https://keycloak.staging.local
    roles:
      - ROLE_admin
      - ROLE_consult
      - ROLE_MAJ
  
  # ========================================
  # Configuration des Utilisateurs de test
  # ========================================
  users:
    - username: ${RHDEMO_TEST_ID_USER_ADMIN}
      password: ${RHDEMO_TEST_PWD_USER_ADMIN}
      email: ${RHDEMO_TEST_ID_USER_ADMIN}@rhdemo.local
      first-name: Admin
      last-name: RHDemo
      roles:
        - ROLE_admin

    - username: ${RHDEMO_TEST_ID_USER_CONSULT}
      password: ${RHDEMO_TEST_PWD_USER_CONSULT}
      email: ${RHDEMO_TEST_ID_USER_CONSULT}@rhdemo.local
      first-name: Jean
      last-name: Consultant
      roles:
        - ROLE_consult

    - username: ${RHDEMO_TEST_ID_USER_MAJ}
      password: ${RHDEMO_TEST_PWD_USER_MAJ}
      email: ${RHDEMO_TEST_ID_USER_MAJ}@rhdemo.local
      first-name: Marie
      last-name: Manager
      roles:
        - ROLE_consult
        - ROLE_MAJ
YMLEOF

                    # Réactiver l'écho maintenant que les secrets ont été écrits dans le fichier
                    set -x

                    echo "✅ Configuration générée: ${CONFIG_FILE}"
                    
                    # Vérifier que les variables sont bien substituées (sans afficher les secrets)
                    echo ""
                    echo "🔍 Vérification de la configuration:"
                    echo "   Root URL:"
                    grep "root-url:" "${CONFIG_FILE}"
                    
                    # Vérifier qu'il n'y a pas de ${VAR} littérales (non substituées)
                    if grep -q '\${' "${CONFIG_FILE}"; then
                        echo "❌ ERREUR: Variables non substituées détectées!"
                        echo "   Variables trouvées:"
                        grep '\${' "${CONFIG_FILE}"
                        exit 1
                    fi
                    
                    echo "✅ Configuration générée avec toutes les variables substituées"
                '''
                
                script {
                    echo '✅ rhDemoInitKeycloak configuré (prêt pour exécution via mvnw)'
                }
            }
        }
        
        stage('�🔍 Vérification Environnement') {
            steps {
                script {
                    echo '▶ Vérification de l\'environnement...'
                    // Charger les variables d'environnement depuis le fichier
                    sh '''
                        # Charger les secrets
                        . rhDemo/secrets/env-vars.sh
                        
                        echo "Java version:"
                        java -version
                        echo "\nMaven version:"
                        cd rhDemo && ./mvnw -version
                        echo "\nVariables d'environnement:"
                        # Compter les variables RHDEMO sans afficher leurs valeurs (sécurité)
                        RHDEMO_VAR_COUNT=$(env | grep -c "^RHDEMO_" || echo "0")
                        echo "✅ $RHDEMO_VAR_COUNT variables RHDEMO configurées (valeurs masquées pour sécurité)"
                    '''
                }
            }
        }
        
        // ════════════════════════════════════════════════════════════════
        // PHASE 2 : BUILD
        // ════════════════════════════════════════════════════════════════
        
        stage('📦 Compilation Backend') {
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 2 : BUILD'
                    echo '═══════════════════════════════════════════════════════'
                    echo '▶ Compilation du backend Spring Boot...'
                }
                sh '''
                    # Charger les secrets pour la compilation
                    . rhDemo/secrets/env-vars.sh
                    cd rhDemo && ./mvnw clean compile -DskipTests
                '''
            }
        }
        
        stage('🎨 Build Frontend Vue.js') {
            steps {
                script {
                    echo '▶ Build du frontend Vue.js via Maven...'
                }
                sh '''
                    echo "Installation Node.js et build Vue.js automatique via frontend-maven-plugin"
                    cd rhDemo/frontend
                    ls -la node/ || echo "Node.js sera installé par Maven"
                '''
            }
        }
        
        stage('🔨 Build') {
            steps {
                script {
                    echo "🔨 Compilation et packaging de l'application..."
                    sh """
                        . rhDemo/secrets/env-vars.sh
                        cd rhDemo && ./mvnw clean package -DskipTests
                    """
                }
            }
        }

        stage('🔒 Analyse Sécurité Dépendances (OWASP)') {
            steps {
                script {
                    echo '▶ Analyse des vulnérabilités des dépendances (OWASP Dependency-Check)...'
                    echo '   ⚠️  Le build échouera si vulnérabilités CVSS ≥ 7.0 (High/Critical)'
                    echo '   📌 Utilisation du plugin Maven OWASP Dependency-Check 12.1.9'

                    // Exécuter OWASP Dependency-Check via Maven
                    dir('rhDemo') {
                        // Tenter d'utiliser à la fois NVD API key et OSS Index credentials
                        def nvdKeyAvailable = false
                        def ossIndexAvailable = false

                        try {
                            withCredentials([string(credentialsId: 'nvd-api-key', variable: 'NVD_API_KEY')]) {
                                nvdKeyAvailable = true
                                echo '   ✅ Clé API NVD configurée'
                            }
                        } catch (Exception e) {
                            echo '   ⚠️  Clé API NVD non configurée - l\'analyse NVD sera plus lente'
                        }

                        try {
                            withCredentials([usernamePassword(credentialsId: 'ossindex-credentials', usernameVariable: 'OSSINDEX_USER', passwordVariable: 'OSSINDEX_PASS')]) {
                                ossIndexAvailable = true
                                echo '   ✅ Credentials OSS Index configurés'
                            }
                        } catch (Exception e) {
                            echo '   ⚠️  Credentials OSS Index non configurés'
                            echo '   💡 Créez un compte sur https://ossindex.sonatype.org/'
                            echo '   💡 Puis créez le credential "ossindex-credentials" (Username with password) dans Jenkins'
                        }

                        // Construire la commande Maven avec les credentials disponibles
                        def mavenCmd = './mvnw org.owasp:dependency-check-maven:check'
                        def credentialsList = []

                        if (nvdKeyAvailable) {
                            credentialsList.add(string(credentialsId: 'nvd-api-key', variable: 'NVD_API_KEY'))
                        }

                        if (ossIndexAvailable) {
                            credentialsList.add(usernamePassword(credentialsId: 'ossindex-credentials', usernameVariable: 'OSSINDEX_USER', passwordVariable: 'OSSINDEX_PASS'))
                        }

                        if (credentialsList.isEmpty()) {
                            echo '   ⚠️  ATTENTION: Aucun credential configuré - l\'analyse sera limitée'
                            sh mavenCmd
                        } else {
                            withCredentials(credentialsList) {
                                if (nvdKeyAvailable && ossIndexAvailable) {
                                    sh './mvnw org.owasp:dependency-check-maven:check -DnvdApiKey="${NVD_API_KEY}" -DossIndexUsername="${OSSINDEX_USER}" -DossIndexPassword="${OSSINDEX_PASS}"'
                                } else if (nvdKeyAvailable) {
                                    sh './mvnw org.owasp:dependency-check-maven:check -DnvdApiKey="${NVD_API_KEY}"'
                                } else if (ossIndexAvailable) {
                                    sh './mvnw org.owasp:dependency-check-maven:check -DossIndexUsername="${OSSINDEX_USER}" -DossIndexPassword="${OSSINDEX_PASS}"'
                                }
                            }
                        }

                        // Afficher un résumé des vulnérabilités et valider le rapport
                        sh '''#!/bin/bash
                            set -e  # Échouer immédiatement en cas d'erreur

                            if [ ! -f "target/dependency-check-report.json" ]; then
                                echo "❌ ERREUR: Rapport JSON non généré"
                                echo "   L'analyse OWASP Dependency-Check a échoué"
                                exit 1
                            fi

                            echo ""
                            echo "📊 Résumé des vulnérabilités détectées:"

                            TOTAL_DEPS=$(jq '.dependencies | length' target/dependency-check-report.json)
                            echo "   Dépendances analysées: ${TOTAL_DEPS}"

                            # Valider que des dépendances ont été analysées
                            if [ "$TOTAL_DEPS" -eq 0 ]; then
                                echo "❌ ERREUR: Aucune dépendance analysée"
                                echo "   Vérifiez la configuration OWASP Dependency-Check"
                                exit 1
                            fi

                            DEPS_WITH_VULNS=$(jq '[.dependencies[] | select(.vulnerabilities != null)] | length' target/dependency-check-report.json)
                            echo "   Dépendances avec vulnérabilités: ${DEPS_WITH_VULNS}"

                            if [ "$DEPS_WITH_VULNS" -gt 0 ]; then
                                CRITICAL=$(jq '[.dependencies[].vulnerabilities[]? | select(.severity == "CRITICAL")] | length' target/dependency-check-report.json)
                                HIGH=$(jq '[.dependencies[].vulnerabilities[]? | select(.severity == "HIGH")] | length' target/dependency-check-report.json)
                                MEDIUM=$(jq '[.dependencies[].vulnerabilities[]? | select(.severity == "MEDIUM")] | length' target/dependency-check-report.json)
                                LOW=$(jq '[.dependencies[].vulnerabilities[]? | select(.severity == "LOW")] | length' target/dependency-check-report.json)

                                echo "   ├─ CRITICAL: ${CRITICAL}"
                                echo "   ├─ HIGH:     ${HIGH}"
                                echo "   ├─ MEDIUM:   ${MEDIUM}"
                                echo "   └─ LOW:      ${LOW}"
                            else
                                echo "   ✅ Aucune vulnérabilité détectée"
                            fi

                            # Vérifier si l'analyse a réellement fonctionné en cherchant les CPE/CVE identifiés
                            TOTAL_CVES=$(jq '[.dependencies[].vulnerabilities[]?.name // empty] | length' target/dependency-check-report.json 2>/dev/null || echo "0")
                            TOTAL_CPES=$(jq '[.dependencies[].packages[]?.id // empty] | length' target/dependency-check-report.json 2>/dev/null || echo "0")

                            echo ""
                            echo "🔍 Validation de l'analyse:"
                            echo "   CVEs identifiées: ${TOTAL_CVES}"
                            echo "   CPEs identifiées: ${TOTAL_CPES}"

                            # Si on a analysé plus de 50 dépendances mais trouvé 0 CVE, c'est suspect
                            if [ "$TOTAL_DEPS" -gt 50 ] && [ "$TOTAL_CVES" -eq 0 ]; then
                                echo ""
                                echo "⚠️  ATTENTION: Analyse suspecte détectée"
                                echo "   ${TOTAL_DEPS} dépendances analysées mais 0 CVE trouvée"
                                echo "   Cela indique probablement un problème de configuration:"
                                echo "   - OSS Index credentials manquants"
                                echo "   - NVD API key manquante ou invalide"
                                echo "   - Cache corrompue"
                                echo ""
                                echo "💡 Actions recommandées:"
                                echo "   1. Configurez les credentials OSS Index (ossindex-credentials)"
                                echo "   2. Vérifiez la clé NVD API (nvd-api-key)"
                                echo "   3. Nettoyez le cache: rm -rf ~/.m2/dependency-check-data"
                                echo ""
                                echo "❌ ÉCHEC: Rapport non fiable - analyse incomplète"
                                exit 1
                            fi

                            echo "✅ Analyse validée avec succès"
                            echo ""
                        '''
                    }
                }

                // Publier les résultats OWASP
                // Note: Le rapport est généré dans rhDemo/target/dependency-check-report.xml
                dependencyCheckPublisher(
                    pattern: 'rhDemo/target/dependency-check-report.xml',
                    failedTotalCritical: 1,    // Échec si ≥ 1 vulnérabilité Critical (CVSS 9.0-10.0)
                    failedTotalHigh: 1          // Échec si ≥ 1 vulnérabilité High (CVSS 7.0-8.9)
                    // Pas de seuils unstable* pour éviter la redondance
                )

                // Archiver aussi les rapports HTML et JSON pour consultation
                archiveArtifacts artifacts: 'rhDemo/target/dependency-check-report.*', fingerprint: true, allowEmptyArchive: true

                // Archiver le log détaillé pour diagnostic
                archiveArtifacts artifacts: 'rhDemo/target/dependency-check.log', fingerprint: true, allowEmptyArchive: true
            }
        }

        stage('🐳 Build Docker Image (Paketo)') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    // Vérifier que APP_VERSION est définie
                    if (!env.APP_VERSION || env.APP_VERSION == '') {
                        error("❌ APP_VERSION non définie ! Le stage 'Lecture Version Maven' a échoué.")
                    }

                    echo "🐳 Construction de l'image Docker avec Paketo Buildpacks..."
                    echo "   Version détectée: ${env.APP_VERSION}"
                    echo "   Image à construire: ${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION}"
                    echo "⚠️  Nettoyage complet: suppression cache Docker + target/ + images existantes..."

                    // Supprimer les anciennes images Docker pour forcer reconstruction
                    sh """
                        docker rmi ${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION} 2>/dev/null || echo "Image n'existe pas encore"
                        docker system prune -f --filter "label=io.buildpacks.stack.id" || true
                    """

                    // CRITIQUE: Supprimer PHYSIQUEMENT target/ avant build image Docker
                    // pour éviter que Paketo réutilise un JAR avec ancienne configuration
                    sh """
                        echo "🗑️  Suppression physique du target/ avant build image..."
                        cd rhDemo && rm -rf target/
                    """

                    sh """
                        . rhDemo/secrets/env-vars.sh
                        cd rhDemo && ./mvnw clean spring-boot:build-image \
                            -Dspring-boot.build-image.imageName=${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION} \
                            -Dspring-boot.build-image.pullPolicy=IF_NOT_PRESENT \
                            -Dspring-boot.build-image.publish=false \
                            -Dspring-boot.build-image.cleanCache=true \
                            -DskipTests
                    """

                    echo "✅ Image Docker créée: ${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION}"

                    // Lister les images créées
                    sh "docker images | grep ${env.DOCKER_IMAGE_NAME} || true"
                }
            }
        }
        
        // ════════════════════════════════════════════════════════════════
        // PHASE 3 : QUALITÉ DU CODE
        // ════════════════════════════════════════════════════════════════
        
        stage('🧪 Tests Unitaires et Intégration') {
            steps {
                script {
                    echo "🧪 Exécution des tests unitaires et d'intégration..."
                    echo "   - Tests unitaires (Surefire)"
                    echo "   - Tests d'intégration (Failsafe)"
                    echo "   - Génération du rapport JaCoCo agrégé pour SonarQube"
                    sh """
                        . rhDemo/secrets/env-vars.sh
                        cd rhDemo && ./mvnw verify
                    """
                }
            }
        }
        
        stage('🔍 Analyse SonarQube') {
            when {
                expression { params.RUN_SONAR == true }
            }
            steps {
                script {
                    echo '▶ Analyse de qualité du code avec SonarQube...'
                    echo '   - Envoi des métriques de couverture (tests unitaires + intégration)'
                }
                withSonarQubeEnv('SonarQube') {
                    sh """
                        # Charger les secrets pour l'analyse
                        . rhDemo/secrets/env-vars.sh
                        cd rhDemo && ./mvnw sonar:sonar \\
                            -Dsonar.projectKey=rhdemo-api \\
                            -Dsonar.projectName="RHDemo API" \\
                            -Dsonar.java.binaries=target/classes \\
                            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
                    """
                }
            }
        }

        stage('🎯 Quality Gate SonarQube') {
            when {
                expression { params.RUN_SONAR == true }
            }
            steps {
                script {
                    echo '▶ Attente du Quality Gate SonarQube...'
                    echo '   (Webhook configuré: http://jenkins:8080/sonarqube-webhook/)'
                }
                timeout(time: 5, unit: 'MINUTES') {
                    script {
                        def qg = waitForQualityGate()
                        if (qg.status != 'OK') {
                            error "Pipeline aborted due to quality gate failure: ${qg.status}"
                        }
                        echo "✅ Quality Gate passed: ${qg.status}"
                    }
                }
            }
        }
        
        stage('📈 Couverture de Code') {
            steps {
                script {
                    echo '▶ Génération du rapport de couverture JaCoCo...'
                }
                sh '''
                    # Charger les secrets pour le rapport
                    . rhDemo/secrets/env-vars.sh
                    cd rhDemo && ./mvnw jacoco:report
                '''
                jacoco(
                    execPattern: '**/rhDemo/target/jacoco.exec',
                    classPattern: '**/rhDemo/target/classes',
                    sourcePattern: '**/rhDemo/src/main/java'
                )
            }
        }
        
        // ════════════════════════════════════════════════════════════════
        // PHASE 4 : ENVIRONNEMENT STAGING (DOCKER COMPOSE)
        // ════════════════════════════════════════════════════════════════
        
        stage('⚙️ Configuration Environnement Test') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 4 : ENVIRONNEMENT STAGING (DOCKER COMPOSE)'
                    echo '═══════════════════════════════════════════════════════'
                    echo '▶ Configuration de l\'environnement de test Docker Compose...'
                    
                    // Nettoyer l'environnement précédent si existant
                    sh """
                        cd ${STAGING_INFRA_PATH}
                        
                        # Arrêter et supprimer les conteneurs du build précédent
                        docker-compose -p ${COMPOSE_PROJECT_NAME} down -v 2>/dev/null || true
                        
                        # Supprimer les volumes orphelins (créés par builds précédents avec noms différents)
                        docker volume rm keycloak-staging-db-data rhdemo-staging-db-data rhdemo-staging-nginx-cache 2>/dev/null || true
                        
                        # IMPORTANT: Supprimer l'image nginx locale pour forcer sa recréation
                        # Évite les problèmes de layers corrompus où nginx.conf a été créé comme répertoire
                        echo "🧹 Suppression image nginx pour éviter layers corrompus..."
                        docker rmi ${NGINX_IMAGE} 2>/dev/null || true
                        
                        # Nettoyer les réseaux orphelins
                        docker network prune -f
                    """
                    
                    // Configuration du fichier hosts (si Jenkins tourne localement)
                    sh """
                        # Vérifier si les entrées hosts existent
                        if ! grep -q "${TEST_DOMAIN}" /etc/hosts 2>/dev/null; then
                            echo "⚠️  ATTENTION: Ajouter '127.0.0.1 ${TEST_DOMAIN} ${KEYCLOAK_DOMAIN}' à /etc/hosts"
                        fi
                    """
                }
            }
        }
        
        stage('🔐 Génération Certificats SSL') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Génération des certificats SSL auto-signés...'
                }
                sh """
                    cd ${STAGING_INFRA_PATH}
                    ./generate-certs.sh --domain ${TEST_DOMAIN} --domain ${KEYCLOAK_DOMAIN}
                """
            }
        }
        
        stage('🏷️ Tag Image Docker') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo "▶ Tag de l'image Docker..."
                    echo "   Source: ${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION}"
                    echo "   Cible:  ${env.RHDEMO_IMAGE}"
                }
                sh """
                    docker tag ${env.DOCKER_IMAGE_NAME}:${env.APP_VERSION} ${env.RHDEMO_IMAGE}
                """
            }
        }
        
        stage('🐳 Démarrage Environnement Docker') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Démarrage de l\'environnement Docker Compose (5 services)...'
                }
                sh """
                    # SÉCURITÉ: Désactiver l'écho des commandes pour ne pas exposer les secrets
                    set +x

                    # Source les secrets SOPS
                    . rhDemo/secrets/env-vars.sh

                    cd ${STAGING_INFRA_PATH}

                    # Variables d'environnement pour Docker Compose
                    # docker-compose.yml utilise image: rhdemo-api:\${APP_VERSION}
                    export APP_VERSION=build-${env.BUILD_NUMBER}
                    export WORKSPACE=\$(pwd)

                    # Export explicite des variables critiques (pour éviter que .env les écrase)
                    export RHDEMO_DB_PASSWORD="\${RHDEMO_DATASOURCE_PASSWORD_PG}"
                    export KEYCLOAK_DB_PASSWORD="\${KEYCLOAK_DB_PASSWORD}"
                    export KEYCLOAK_ADMIN_USER="\${KEYCLOAK_ADMIN_USER}"
                    export KEYCLOAK_ADMIN_PASSWORD="\${KEYCLOAK_ADMIN_PASSWORD}"
                    export RHDEMO_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET="\${RHDEMO_CLIENT_REGISTRATION_KEYCLOAK_CLIENT_SECRET}"

                    # Réactiver l'écho APRÈS les exports de secrets
                    set -x

                    # Confirmation (sans afficher les secrets)
                    echo "✅ Secrets exportés avec succès (secrets non affichés pour sécurité)"
                    
                    # IMPORTANT: Nettoyage forcé des conteneurs existants pour éviter les conflits de noms
                    echo "🧹 Nettoyage des conteneurs staging existants..."
                    docker rm -f keycloak-staging-db rhdemo-staging-db keycloak-staging rhdemo-staging-app rhdemo-staging-nginx 2>/dev/null || true
                    echo "✅ Conteneurs existants supprimés"
                    
                    # Démarrer les conteneurs (plus besoin de docker-compose.jenkins.yml override)
                    echo "🚀 Démarrage des conteneurs Docker..."
                    docker-compose -f docker-compose.yml -p ${COMPOSE_PROJECT_NAME} up -d
                    
                    # Connecter Jenkins au réseau staging pour accès direct aux services
                    echo "🔗 Connexion de Jenkins au réseau staging..."
                    # Trouver le conteneur Jenkins principal (pas l'agent)
                    JENKINS_CONTAINER=\$(docker ps --filter "name=jenkins" --format "{{.Names}}" | grep -v agent | head -n 1)
                    echo "Conteneur Jenkins trouvé: \$JENKINS_CONTAINER"
                    if [ -n "\$JENKINS_CONTAINER" ]; then
                        docker network connect rhdemo-staging-network \$JENKINS_CONTAINER 2>/dev/null || echo "⚠️  Jenkins déjà connecté au réseau"
                        echo "✅ Jenkins (\$JENKINS_CONTAINER) connecté au réseau rhdemo-staging-network"
                    else
                        echo "❌ ERREUR: Conteneur Jenkins introuvable!"
                        docker ps --filter "name=jenkins"
                    fi
                    
                    # Attendre que les conteneurs démarrent (augmenté pour Keycloak)
                    echo "⏳ Attente démarrage conteneurs (20s)..."
                    sleep 20
                    
                    # Copier les configurations nginx et certificats SSL
                    echo "📋 Copie des configurations nginx..."
                    docker cp nginx/nginx.conf rhdemo-staging-nginx:/etc/nginx/nginx.conf
                    docker cp nginx/conf.d/. rhdemo-staging-nginx:/etc/nginx/conf.d/
                    
                    if [ -d "certs" ]; then
                        docker cp certs/. rhdemo-staging-nginx:/etc/nginx/ssl/
                        echo "✅ Configurations nginx et certificats copiés"
                    else
                        echo "✅ Configurations nginx copiées (certificats manquants)"
                    fi
                    
                    # Recharger la configuration nginx pour appliquer les changements
                    echo "🔄 Rechargement de la configuration nginx..."
                    docker exec rhdemo-staging-nginx nginx -t  # Test de la config
                    docker exec rhdemo-staging-nginx nginx -s reload  # Reload
                    echo "✅ Nginx rechargé avec la nouvelle configuration HTTPS"
                    
                    # Vérifier que nginx écoute réellement sur le port 443
                    echo "🔍 Vérification que nginx écoute sur le port 443..."
                    if docker exec rhdemo-staging-nginx netstat -tuln | grep -q ':443'; then
                        echo "✅ Nginx écoute sur le port 443 (HTTPS)"
                    else
                        echo "❌ ERREUR: Nginx n'écoute PAS sur le port 443!"
                        echo "Ports écoutés par nginx:"
                        docker exec rhdemo-staging-nginx netstat -tuln
                        exit 1
                    fi
                    
                    echo "✅ Environnement Docker opérationnel:"
                    docker-compose -p ${COMPOSE_PROJECT_NAME} ps
                """
            }
        }

        stage('🔐 Injection secrets rhDemo dans container') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Copie du fichier secrets-rhdemo.yml dans le container rhDemo...'
                    echo '   Ce fichier contient uniquement les secrets nécessaires à rhDemo (moindre privilège)'
                }
                sh """
                    cd ${STAGING_INFRA_PATH}

                    # Chemin du fichier secrets dans le workspace Jenkins
                    SECRETS_FILE="../../secrets/secrets-rhdemo.yml"

                    if [ ! -f "\$SECRETS_FILE" ]; then
                        echo "❌ Erreur: Fichier secrets-rhdemo.yml introuvable"
                        echo "Recherche dans le workspace:"
                        find ../../ -name "secrets-rhdemo.yml" || echo "Aucun fichier trouvé"
                        exit 1
                    fi

                    echo "📋 Copie du fichier secrets dans le container..."

                    # Créer le répertoire en tant que root, puis changer le propriétaire
                    # L'utilisateur CNB (Cloud Native Buildpacks) n'a pas les droits root
                    docker exec --user root rhdemo-staging-app mkdir -p /workspace/secrets
                    docker exec --user root rhdemo-staging-app chown cnb:cnb /workspace/secrets

                    # Copier le fichier
                    docker cp "\$SECRETS_FILE" rhdemo-staging-app:/workspace/secrets/secrets-rhdemo.yml

                    # Définir les permissions (read-only) et le propriétaire en tant que root
                    docker exec --user root rhdemo-staging-app chown cnb:cnb /workspace/secrets/secrets-rhdemo.yml
                    docker exec --user root rhdemo-staging-app chmod 400 /workspace/secrets/secrets-rhdemo.yml

                    echo "✅ Fichier secrets-rhdemo.yml copié dans le container"
                    echo "   Chemin: /workspace/secrets/secrets-rhdemo.yml"
                    echo "   Permissions: 400 (read-only, propriétaire: cnb)"

                    # Vérification (sans afficher le contenu pour sécurité)
                    docker exec rhdemo-staging-app ls -lh /workspace/secrets/secrets-rhdemo.yml

                    # Redémarrer l'application pour charger les secrets
                    # Spring Boot charge spring.config.import au démarrage uniquement
                    echo "🔄 Redémarrage de l'application pour charger les secrets..."
                    docker-compose -f docker-compose.yml -p ${COMPOSE_PROJECT_NAME} restart rhdemo-app
                    echo "✅ Application redémarrée (healthcheck dans le stage dédié)"
                """
            }
        }

        stage('💾 Initialisation Base de Données') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Initialisation de la base de données PostgreSQL...'
                }
                sh """
                    cd ${STAGING_INFRA_PATH}
                    
                    # Attendre que PostgreSQL soit prêt
                    echo "⏳ Attente PostgreSQL ready (30s max)..."
                    timeout=30
                    while [ \$timeout -gt 0 ]; do
                        if docker exec rhdemo-staging-db pg_isready -U rhdemo >/dev/null 2>&1; then
                            echo "✅ PostgreSQL ready"
                            break
                        fi
                        sleep 2
                        timeout=\$((timeout - 2))
                    done
                    
                    if [ \$timeout -le 0 ]; then
                        echo "❌ Timeout: PostgreSQL n'est pas prêt après 30s"
                        docker logs --tail=20 rhdemo-staging-db
                        exit 1
                    fi
                    
                    # Copier et exécuter le schéma SQL
                    PGDDL_PATH="../../pgddl.sql"
                    if [ ! -f "\$PGDDL_PATH" ]; then
                        echo "❌ Erreur: Fichier pgddl.sql introuvable"
                        exit 1
                    fi
                    
                    echo "📋 Copie du schéma SQL dans le container..."
                    docker cp "\$PGDDL_PATH" rhdemo-staging-db:/tmp/schema.sql
                    
                    echo "🔧 Exécution du schéma SQL (DROP + CREATE + INSERT)..."
                    docker exec rhdemo-staging-db psql -U rhdemo -d rhdemo -f /tmp/schema.sql
                    
                    # Vérifier le nombre d'employés insérés
                    EMPLOYEE_COUNT=\$(docker exec rhdemo-staging-db psql -U rhdemo -d rhdemo -t -c "SELECT COUNT(*) FROM employes;" | tr -d ' ')
                    
                    echo "✅ Base de données initialisée"
                    echo "   📊 Nombre d'employés: \$EMPLOYEE_COUNT"
                    
                    if [ "\$EMPLOYEE_COUNT" != "303" ]; then
                        echo "⚠️  Attention: Attendu 303 employés, trouvé \$EMPLOYEE_COUNT"
                    fi
                """
            }
        }
        
        stage('🏥 Healthcheck Keycloak') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Attente démarrage Keycloak...'

                    // Charger la bibliothèque
                    def lib = load 'rhDemo/vars/rhDemoLib.groovy'

                    // Healthcheck Keycloak avec attente initiale
                    lib.waitForHealthcheck([
                        name: 'Keycloak',
                        url: "http://${env.CONTAINER_KEYCLOAK}:9000/health/ready",
                        timeout: 60,
                        initialWait: 45,
                        container: env.CONTAINER_KEYCLOAK
                    ])
                }
            }
        }
        
        stage('🔑 Initialisation Keycloak') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Initialisation du realm Keycloak...'
                    echo 'Création du realm RHDemo, client et utilisateurs'
                }
                sh """
                    # SÉCURITÉ: Désactiver l'écho des commandes pour ne pas exposer les secrets
                    set +x

                    # Source les secrets
                    . rhDemo/secrets/env-vars.sh

                    # Réactiver l'écho pour les commandes non sensibles
                    set -x

                    echo "🚀 Exécution de rhDemoInitKeycloak via Maven..."
                    echo "   (Utilise directement application-staging.yml généré, sans packaging)"
                    
                    cd ${WORKSPACE}/rhDemoInitKeycloak
                    
                    # Vérification que le fichier de config existe
                    CONFIG_FILE="src/main/resources/application-staging.yml"
                    if [ ! -f "\${CONFIG_FILE}" ]; then
                        echo "❌ Fichier de configuration manquant: \${CONFIG_FILE}"
                        exit 1
                    fi
                    
                    echo "� Configuration trouvée: \${CONFIG_FILE}"
                    echo ""
                    echo "✅ Configuration Keycloak préparée (secrets non affichés pour sécurité)"
                    echo ""

                    # Exécution directe via Maven Spring Boot plugin
                    if ./mvnw spring-boot:run -Dspring-boot.run.profiles=staging; then
                        echo ""
                        echo "✅ Keycloak initialisé avec succès"
                        echo "   - Realm RHDemo créé"
                        echo "   - Client RHDemo configuré"
                        echo "   - 3 utilisateurs créés (admin, consultant, manager)"
                    else
                        echo ""
                        echo "❌ Échec de l'initialisation Keycloak"
                        echo ""
                        echo "📋 Logs Keycloak pour diagnostic:"
                        docker logs --tail=30 keycloak-staging || true
                        exit 1
                    fi
                    
                    # Attendre propagation de la configuration
                    echo ""
                    echo "⏳ Attente propagation config Keycloak (10s)..."
                    sleep 10
                """
            }
        }
        
        stage('🏥 Healthcheck Application RHDemo') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Vérification santé de l\'application RHDemo...'

                    // Charger la bibliothèque
                    def lib = load 'rhDemo/vars/rhDemoLib.groovy'

                    // Healthcheck RHDemo App (accepte 301/302 car redirection OAuth2)
                    lib.waitForHealthcheck([
                        name: 'RHDemo App',
                        url: "http://${env.CONTAINER_APP}:9000/front/",
                        timeout: 90,
                        acceptedCodes: [200, 301, 302],
                        container: env.CONTAINER_APP
                    ])
                }
            }
        }
        
        stage('🌐 Healthcheck Nginx HTTPS') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            steps {
                script {
                    echo '▶ Vérification de l\'accès HTTPS via nginx (test end-to-end)...'

                    // Charger la bibliothèque
                    def lib = load 'rhDemo/vars/rhDemoLib.groovy'

                    // Healthcheck Nginx HTTPS (test depuis Jenkins où Firefox tourne)
                    lib.waitForHealthcheck([
                        name: 'Nginx HTTPS',
                        url: 'https://rhdemo.staging.local/front/',
                        timeout: 30,
                        acceptedCodes: [200, 302],
                        insecure: true,  // Certificat auto-signé
                        container: env.CONTAINER_NGINX
                    ])

                    echo "✅ Stack complète opérationnelle et accessible via HTTPS"
                    echo "   Prêt pour les tests Selenium"
                }
            }
        }

        stage('🔍 Scan Sécurité Images Docker (Trivy)') {
            when {
                expression { params.DEPLOY_ENV != 'none' }
            }
            options {
                timeout(time: 8, unit: 'MINUTES')  // Réduit de 15min à 8min grâce à la parallélisation
            }
            steps {
                script {
                    echo '▶ Scan de sécurité des images Docker avec Trivy (parallélisé)...'
                    echo '   ℹ️  Les 4 images seront scannées en parallèle pour optimiser le temps'
                    echo ''
                    echo '   ⚠️  Le build échouera si vulnérabilités CRITICAL détectées'
                    echo '   ⏱️  Timeout: 8 minutes (au lieu de 20 min séquentiel)'

                    // Charger la bibliothèque
                    def lib = load 'rhDemo/vars/rhDemoLib.groovy'

                    // Créer le répertoire pour les rapports
                    sh 'mkdir -p trivy-reports'

                    // Définir les images à scanner avec leurs noms de rapports
                    def imagesToScan = [
                        [image: env.POSTGRES_IMAGE, name: 'postgres'],
                        [image: env.KEYCLOAK_IMAGE, name: 'keycloak'],
                        [image: env.NGINX_IMAGE, name: 'nginx'],
                        [image: env.RHDEMO_IMAGE, name: 'rhdemo-app']
                    ]

                    echo "📋 Images à scanner:"
                    imagesToScan.each { img ->
                        echo "   - ${img.name}: ${img.image}"
                    }

                    // Créer les stages parallèles de scan
                    def scanStages = imagesToScan.collectEntries { img ->
                        ["Scan ${img.name}": {
                            lib.generateTrivyReport(img.image, img.name)
                        }]
                    }

                    // Exécuter tous les scans en parallèle
                    parallel(scanStages + [failFast: false])

                    // Agrégation des résultats et vérification des seuils
                    if (!lib.aggregateTrivyResults()) {
                        error("Trivy a détecté des vulnérabilités CRITICAL bloquantes")
                    }
                }

                // Archiver les rapports Trivy (JSON et HTML)
                archiveArtifacts artifacts: 'trivy-reports/*.json', fingerprint: true, allowEmptyArchive: true
                archiveArtifacts artifacts: 'trivy-reports/*.html', fingerprint: true, allowEmptyArchive: true
            }
        }

        // ════════════════════════════════════════════════════════════════
        // PHASE 5 : TESTS FONCTIONNELS
        // ════════════════════════════════════════════════════════════════

        stage('🔒 Démarrage OWASP ZAP Proxy') {
            when {
                allOf {
                    expression { params.DEPLOY_ENV != 'none' }
                    expression { params.RUN_SELENIUM_TESTS == true }
                }
            }
            steps {
                script {
                    echo '▶ Démarrage du proxy OWASP ZAP pour analyse de sécurité...'
                    echo '   Le trafic Selenium sera intercepté et analysé par ZAP'
                }

                sh """#!/bin/bash
                    # Utiliser la clé API ZAP définie comme variable d'environnement Jenkins
                    echo "🔐 Utilisation de ZAP_API_KEY: ${env.ZAP_API_KEY.take(8)}..."

                    # IMPORTANT: ZAP est maintenant dans l'environnement Jenkins, pas staging
                    cd rhDemo/infra/jenkins-docker

                    # Nettoyage : supprimer le conteneur ZAP existant s'il existe
                    if docker ps -a --format '{{.Names}}' | grep -q '^rhdemo-jenkins-zap\$'; then
                        echo "🧹 Suppression du conteneur ZAP existant..."
                        docker stop rhdemo-jenkins-zap 2>/dev/null || true
                        docker rm rhdemo-jenkins-zap 2>/dev/null || true
                        echo "✅ Conteneur ZAP supprimé"
                    fi

                    # Démarrage d'un nouveau conteneur ZAP avec la clé API correcte
                    echo "🚀 Démarrage d'un nouveau conteneur ZAP..."
                    echo "   Réseau: rhdemo-jenkins-network"
                    echo "   Clé API: ${env.ZAP_API_KEY.take(8)}..."
                    ZAP_API_KEY=${env.ZAP_API_KEY} docker-compose -f docker-compose.yml -f docker-compose.zap.yml up -d owasp-zap

                    echo "⏳ Attente du démarrage de ZAP (30s max)..."
                    timeout=30
                    while [ \$timeout -gt 0 ]; do
                        if docker exec rhdemo-jenkins-zap curl -f http://localhost:8090/JSON/core/view/version/?apikey=${env.ZAP_API_KEY} >/dev/null 2>&1; then
                            echo "✅ ZAP démarré et accessible"
                            break
                        fi
                        sleep 2
                        timeout=\$((timeout - 2))
                    done

                    if [ \$timeout -le 0 ]; then
                        echo "❌ Timeout: ZAP n'est pas prêt après 30s"
                        docker logs --tail=20 rhdemo-jenkins-zap
                        exit 1
                    fi

                    echo "📊 ZAP Status:"
                    docker exec rhdemo-jenkins-zap curl -s http://localhost:8090/JSON/core/view/version/?apikey=${env.ZAP_API_KEY} | jq .

                    echo ""
                    echo "🔗 Connexion de ZAP au réseau staging..."
                    docker network connect rhdemo-staging-network rhdemo-jenkins-zap 2>/dev/null || echo "⚠️  ZAP déjà connecté au réseau staging"

                    echo ""
                    echo "✅ OWASP ZAP prêt à intercepter le trafic Selenium"
                    echo "   Container: rhdemo-jenkins-zap"
                    echo "   Réseau 1: rhdemo-jenkins-network (API + Proxy)"
                    echo "   Réseau 2: rhdemo-staging-network (accès application via connexion dynamique)"
                    echo "   Port unique (API + Proxy): owasp-zap:8090"
                """
            }
        }

        stage('🌐 Tests Selenium IHM') {
            when {
                allOf {
                    expression { params.DEPLOY_ENV != 'none' }
                    expression { params.RUN_SELENIUM_TESTS == true }
                }
            }
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 5 : TESTS FONCTIONNELS'
                    echo '═══════════════════════════════════════════════════════'
                    echo '▶ Exécution des tests Selenium sur environnement Docker Compose...'
                }
                
                dir(TEST_PROJECT_PATH) {
                    sh """
                        . \${WORKSPACE}/rhDemo/secrets/env-vars.sh

                        # URLs via nginx dans le réseau Docker avec HTTPS
                        # Firefox dans Jenkins doit utiliser les network aliases DNS Docker
                        # Car Jenkins container est connecté à rhdemo-staging-network
                        # Les aliases rhdemo.staging.local et keycloak.staging.local sont résolus via DNS Docker
                        # HTTPS utilisé pour tester en conditions réelles (certificat auto-signé accepté par setAcceptInsecureCerts)
                        APP_URL="https://rhdemo.staging.local"
                        KEYCLOAK_URL="https://rhdemo.staging.local/realms/RHDemo"

                        # Credentials de test (user avec ROLE_consult + ROLE_MAJ pour tester toutes les opérations)
                        # Ces variables seront passées via propriétés Maven à TestConfig
                        # Username chargé depuis secrets-staging.yml (pas de valeur en dur)
                        TEST_USERNAME="\${RHDEMO_TEST_ID_USER_MAJ}"
                        TEST_PASSWORD="\${RHDEMO_TEST_PWD_USER_MAJ}"

                        # Configuration du proxy ZAP pour intercepter le trafic Selenium
                        # BaseSeleniumTest.java détecte ces variables et configure le proxy WebDriver
                        export ZAP_PROXY_HOST=owasp-zap
                        export ZAP_PROXY_PORT=8090

                        echo "📍 Configuration des tests Selenium:"
                        echo "   APP_URL: \${APP_URL}"
                        echo "   KEYCLOAK_URL: \${KEYCLOAK_URL}"
                        echo "   TEST_USERNAME: \${TEST_USERNAME} (ROLE_consult + ROLE_MAJ)"
                        echo "   Mode: Headless"
                        echo "   ZAP Proxy: \${ZAP_PROXY_HOST}:\${ZAP_PROXY_PORT}"
                        echo "   (Healthcheck HTTPS validé avant ce stage)"

                        # Lancement des tests avec TOUTES les configs via propriétés Maven
                        # TestConfig charge dans l'ordre: Maven properties > env vars > YAML
                        cd \${WORKSPACE}/rhDemo && ./mvnw clean test -f ../\${TEST_PROJECT_PATH}/pom.xml \\
                            -Dtest.baseurl=\${APP_URL} \\
                            -Dtest.keycloak.url=\${KEYCLOAK_URL} \\
                            -Dselenium.headless=true \\
                            -Dtest.username=\${TEST_USERNAME} \\
                            -Dtest.password=\${TEST_PASSWORD}
                    """
                }
            }
            post {
                always {
                    script {
                        // Archivage des screenshots en cas d'échec
                        dir(TEST_PROJECT_PATH) {
                            archiveArtifacts artifacts: '**/screenshots/**/*.png', allowEmptyArchive: true
                            junit '**/target/surefire-reports/*.xml'
                        }

                        // Génération du rapport OWASP ZAP avant déconnexion
                        echo '📊 Génération du rapport OWASP ZAP...'
                        sh """#!/bin/bash
                            # Sauvegarder le workspace actuel
                            WORKSPACE_DIR=\$(pwd)

                            # Créer le répertoire pour les rapports ZAP dans le workspace
                            mkdir -p "\${WORKSPACE_DIR}/zap-reports"

                            # Utiliser la clé API ZAP définie comme variable d'environnement Jenkins
                            ZAP_API_KEY="${env.ZAP_API_KEY}"
                            ZAP_HOST="owasp-zap"
                            ZAP_PORT="8090"

                            echo "🔍 Récupération du rapport ZAP depuis http://\${ZAP_HOST}:\${ZAP_PORT}"
                            echo "   Clé API utilisée: \${ZAP_API_KEY:0:8}..."

                            # Tester d'abord la connexion à ZAP
                            if curl -s -f "http://\${ZAP_HOST}:\${ZAP_PORT}/JSON/core/view/version/?apikey=\${ZAP_API_KEY}" >/dev/null 2>&1; then
                                echo "✅ Connexion à ZAP réussie"

                                # Générer le rapport HTML
                                if curl -f -o "\${WORKSPACE_DIR}/zap-reports/zap-report.html" \
                                    "http://\${ZAP_HOST}:\${ZAP_PORT}/OTHER/core/other/htmlreport/?apikey=\${ZAP_API_KEY}" 2>&1; then
                                    echo "✅ Rapport HTML ZAP généré: \$(du -h \${WORKSPACE_DIR}/zap-reports/zap-report.html | cut -f1)"
                                else
                                    echo "⚠️  Échec de génération du rapport HTML ZAP"
                                fi

                                # Générer également le rapport JSON pour analyse
                                if curl -f -o "\${WORKSPACE_DIR}/zap-reports/zap-report.json" \
                                    "http://\${ZAP_HOST}:\${ZAP_PORT}/JSON/core/view/alerts/?apikey=\${ZAP_API_KEY}" 2>&1; then
                                    ALERT_COUNT=\$(jq '. | length' "\${WORKSPACE_DIR}/zap-reports/zap-report.json" 2>/dev/null || echo "?")
                                    echo "✅ Rapport JSON ZAP généré: \${ALERT_COUNT} alertes détectées"
                                else
                                    echo "⚠️  Échec de génération du rapport JSON ZAP"
                                fi
                            else
                                echo "❌ Impossible de se connecter à ZAP - Vérifier que ZAP est démarré et que la clé API est correcte"
                            fi
                        """

                        // Archiver les rapports ZAP
                        archiveArtifacts artifacts: 'zap-reports/*', fingerprint: true, allowEmptyArchive: true

                        // Déconnexion de ZAP du réseau staging après les tests
                        echo '🔌 Déconnexion de ZAP du réseau staging...'
                        sh '''
                            docker network disconnect rhdemo-staging-network rhdemo-jenkins-zap 2>/dev/null || echo "⚠️  ZAP déjà déconnecté du réseau staging"
                            echo "✅ ZAP déconnecté du réseau staging (reste sur jenkins-network)"
                        '''

                        // Déconnexion de Jenkins du réseau staging après les tests
                        echo '🔌 Déconnexion de Jenkins du réseau staging...'
                        sh '''#!/bin/bash
                            # Trouver le conteneur Jenkins principal (pas l'agent)
                            JENKINS_CONTAINER=$(docker ps --filter "name=jenkins" --format "{{.Names}}" | grep -v agent | head -n 1)
                            echo "Conteneur Jenkins trouvé: $JENKINS_CONTAINER"

                            if [ -n "$JENKINS_CONTAINER" ]; then
                                docker network disconnect rhdemo-staging-network $JENKINS_CONTAINER 2>/dev/null || echo "⚠️  Jenkins déjà déconnecté du réseau staging"
                                echo "✅ Jenkins ($JENKINS_CONTAINER) déconnecté du réseau rhdemo-staging-network"
                            else
                                echo "⚠️ Conteneur Jenkins introuvable pour déconnexion"
                            fi
                        '''
                    }
                }
            }
        }
        
        stage('🛑 Arrêt App Test') {
            when {
                allOf {
                    expression { params.DEPLOY_ENV != 'none' }
                    expression { params.RUN_SELENIUM_TESTS == true }
                }
            }
            steps {
                script {
                    echo '▶ Arrêt de l\'application test...'
                    sh '''
                        if [ -f app-test.container ]; then
                            CONTAINER=$(cat app-test.container)
                            echo "Arrêt du container: $CONTAINER"
                            docker stop $CONTAINER 2>/dev/null || echo "✅ Container déjà arrêté"
                            docker rm $CONTAINER 2>/dev/null || echo "✅ Container déjà supprimé"
                            rm app-test.container
                        else
                            echo "✅ Pas de container de test à nettoyer"
                        fi
                    '''
                }
            }
        }

        // ════════════════════════════════════════════════════════════════
        // PHASE 6 : ARCHIVAGE
        // ════════════════════════════════════════════════════════════════
        
        stage('📚 Archivage Artifacts') {
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 6 : ARCHIVAGE'
                    echo '═══════════════════════════════════════════════════════'
                    echo '▶ Archivage du JAR et des rapports...'
                }
                archiveArtifacts artifacts: "${JAR_FILE}", fingerprint: true
                archiveArtifacts artifacts: '**/target/surefire-reports/**', allowEmptyArchive: true
            }
        }
        
        stage('📝 Génération Rapports') {
            steps {
                script {
                    echo '▶ Publication des rapports...'

                    // Utilisation de la fonction centralisée pour publier tous les rapports
                    def lib = load 'rhDemo/vars/rhDemoLib.groovy'

                    def reports = [
                        ['rhDemo/target/site/jacoco', 'index.html', 'Code Coverage (JaCoCo)'],
                        ['trivy-reports', 'nginx.html', 'Trivy - Nginx'],
                        ['trivy-reports', 'postgres.html', 'Trivy - PostgreSQL'],
                        ['trivy-reports', 'keycloak.html', 'Trivy - Keycloak'],
                        ['trivy-reports', 'rhdemo-app.html', 'Trivy - RHDemo App'],
                        ['rhDemo/target', 'dependency-check-report.html', 'OWASP Dependency-Check'],
                        ['zap-reports', 'zap-report.html', 'OWASP ZAP Security Report']
                    ]

                    lib.publishHTMLReports(reports)
                }
            }
        }
        
        // ════════════════════════════════════════════════════════════════
        // PHASE 7 : PRODUCTION (Simulation)
        // ════════════════════════════════════════════════════════════════
        
        stage('✋ Validation Manuelle') {
            when {
                expression { 
                    params.DEPLOY_ENV == 'production' && params.SKIP_MANUAL_APPROVAL == false 
                }
            }
            steps {
                script {
                    echo '═══════════════════════════════════════════════════════'
                    echo '  PHASE 7 : PRODUCTION (Simulation)'
                    echo '═══════════════════════════════════════════════════════'
                    echo '▶ Approbation manuelle requise pour la production...'
                }
                timeout(time: 30, unit: 'MINUTES') {
                    input message: 'Déployer en production (SIMULATION) ?', 
                          ok: 'Déployer',
                          submitter: 'admin,deploy-team'
                }
            }
        }
        
        stage('💾 Backup Base de Données (SIMULATION)') {
            when {
                expression { params.DEPLOY_ENV == 'production' }
            }
            steps {
                script {
                    echo '▶ Sauvegarde de la base de données de production (SIMULATION)...'
                }
                sh '''
                    # Charger les secrets
                    . rhDemo/secrets/env-vars.sh
                    
                    BACKUP_FILE="db-backup-$(date +%Y%m%d-%H%M%S).sql"
                    echo "Création du backup: ${BACKUP_FILE}"
                    # pg_dump -h ${PROD_SERVER} -U rhdemo rhdemo > ${BACKUP_FILE}
                    echo "✅ Backup créé (SIMULATION)"
                '''
            }
        }
        
        stage('🌐 Déploiement Production (SIMULATION)') {
            when {
                expression { params.DEPLOY_ENV == 'production' }
            }
            steps {
                script {
                    echo '▶ Déploiement en PRODUCTION (SIMULATION)...'
                    echo '🚀 Déploiement Blue-Green...'
                }
                sh '''
                    # Charger les secrets et URLs de serveurs
                    . rhDemo/secrets/env-vars.sh
                    
                    # Stratégie Blue-Green deployment
                    echo "1. Déploiement sur le serveur Green..."
                    # scp ${JAR_FILE} user@${PROD_SERVER}-green:/opt/rhdemo/
                    
                    echo "2. Démarrage du nouveau service..."
                    # ssh user@${PROD_SERVER}-green "systemctl start rhdemo-api"
                    
                    echo "3. Vérification du healthcheck..."
                    # curl -f http://${PROD_SERVER}-green/actuator/health
                    
                    echo "4. Basculement du load balancer..."
                    # Commande pour basculer le trafic
                    
                    echo "5. Arrêt de l'ancien service (Blue)..."
                    # ssh user@${PROD_SERVER}-blue "systemctl stop rhdemo-api"
                    
                    echo "✅ Déploiement production (SIMULATION)"
                '''
            }
        }
        
        stage('✅ Vérification Post-Déploiement (SIMULATION)') {
            when {
                expression { params.DEPLOY_ENV == 'production' }
            }
            steps {
                script {
                    echo '▶ Vérification du déploiement production...'
                }
                sh '''
                    # Charger les secrets et URLs de serveurs
                    . rhDemo/secrets/env-vars.sh
                    
                    echo "Healthcheck application..."
                    # curl -f http://${PROD_SERVER}/actuator/health
                    
                    echo "Vérification des métriques Prometheus..."
                    # curl -f http://${PROD_SERVER}/actuator/prometheus
                    
                    echo "Test des endpoints critiques..."
                    # Tests automatisés sur la production
                    
                    echo "✅ Application en production fonctionnelle (SIMULATION)"
                '''
            }
        }
    }
    
    // ════════════════════════════════════════════════════════════════
    // POST-ACTIONS
    // ════════════════════════════════════════════════════════════════
    
    post {
        always {
            script {
                echo '═══════════════════════════════════════════════════════'
                echo '  NETTOYAGE ET NOTIFICATIONS'
                echo '═══════════════════════════════════════════════════════'
                
                // Pas de nettoyage DNS nécessaire: les alias Docker network
                // sont automatiquement supprimés avec le docker-compose down
                echo '✅ Résolution DNS via Docker network aliases (pas de nettoyage nécessaire)'

                // SÉCURITÉ : Nettoyage des fichiers contenant des secrets
                echo '▶ Nettoyage sécurisé des fichiers de secrets...'
                sh '''
                    # Écraser puis supprimer les fichiers contenant des secrets
                    if [ -f "rhDemo/secrets/env-vars.sh" ]; then
                        # Écraser avec des zéros avant suppression (sécurité supplémentaire)
                        shred -vfz -n 3 rhDemo/secrets/env-vars.sh 2>/dev/null || rm -f rhDemo/secrets/env-vars.sh
                        echo "✅ env-vars.sh supprimé de manière sécurisée"
                    fi

                    if [ -f "rhDemo/secrets/secrets-rhdemo.yml" ]; then
                        # Nettoyer aussi le fichier secrets-rhdemo.yml généré pour rhDemo (contient secrets limités)
                        shred -vfz -n 3 rhDemo/secrets/secrets-rhdemo.yml 2>/dev/null || rm -f rhDemo/secrets/secrets-rhdemo.yml
                        echo "✅ secrets-rhdemo.yml (rhDemo) supprimé de manière sécurisée"
                    fi

                    if [ -f "${SECRETS_DECRYPTED}" ]; then
                        shred -vfz -n 3 ${SECRETS_DECRYPTED} 2>/dev/null || rm -f ${SECRETS_DECRYPTED}
                        echo "✅ secrets-decrypted.yml supprimé de manière sécurisée"
                    fi

                    # Nettoyer aussi le fichier de config Keycloak généré
                    if [ -f "rhDemoInitKeycloak/src/main/resources/application-staging.yml" ]; then
                        shred -vfz -n 3 rhDemoInitKeycloak/src/main/resources/application-staging.yml 2>/dev/null || \
                        rm -f rhDemoInitKeycloak/src/main/resources/application-staging.yml
                        echo "✅ application-staging.yml supprimé de manière sécurisée"
                    fi
                '''

                // Conservation de l'environnement Docker Compose pour tests manuels
                if (params.RUN_SELENIUM_TESTS == true) {
                    def buildResult = currentBuild.result ?: 'SUCCESS'

                    if (buildResult != 'SUCCESS') {
                        echo "📋 Récupération des logs Docker (build ${buildResult})..."
                        sh """
                            cd ${STAGING_INFRA_PATH} || exit 0
                            docker-compose -p ${COMPOSE_PROJECT_NAME} logs --tail=100 || true
                        """
                    }

                    echo '✅ Environnement staging conservé pour tests manuels'
                    echo '📍 Accès à l\'environnement:'
                    echo '   - Application rhDemo : https://rhdemo.staging.local'
                    echo '   - Keycloak Admin     : https://keycloak.staging.local'
                    echo ''
                    echo '🔧 Commandes utiles:'
                    echo "   - Logs app     : docker logs -f rhdemo-staging-app"
                    echo "   - Logs Keycloak: docker logs -f keycloak-staging"
                    echo "   - Arrêter      : cd ${STAGING_INFRA_PATH} && docker-compose -p ${COMPOSE_PROJECT_NAME} down -v"
                    echo ''
                    echo '⚠️  IMPORTANT: L\'environnement sera automatiquement nettoyé au prochain build'
                    echo '   Le nettoyage se fait en début de pipeline (stage "Configuration Environnement Test")'
                }

                echo 'Note: Le nettoyage du workspace et de Docker se fait automatiquement au début du prochain build'
            }
        }
        
        success {
            script {
                def deployInfo = params.DEPLOY_ENV != 'none' ? " et déployé en ${params.DEPLOY_ENV}" : ""
                echo "✅ Pipeline réussi${deployInfo} !"
            }
            
            // Email de notification
            emailext(
                subject: "✅ BUILD SUCCESS - RHDemo API #${env.BUILD_NUMBER}",
                body: """
                    Le build de RHDemo API a réussi !
                    
                    Branch: ${env.BRANCH_NAME}
                    Commit: ${env.GIT_COMMIT}
                    Déploiement: ${params.DEPLOY_ENV}
                    
                    Voir les détails: ${env.BUILD_URL}
                """,
                to: "${env.NOTIFICATION_EMAIL}"
            )
        }
        
        failure {
            script {
                echo "❌ Pipeline échoué !"
            }
            
            // Email d'alerte
            emailext(
                subject: "❌ BUILD FAILED - RHDemo API #${env.BUILD_NUMBER}",
                body: """
                    ⚠️ Le build de RHDemo API a échoué !
                    
                    Branch: ${env.BRANCH_NAME}
                    Stage: ${env.STAGE_NAME}
                    
                    Voir les logs: ${env.BUILD_URL}console
                """,
                to: "${env.NOTIFICATION_EMAIL}",
                attachLog: true
            )
        }
        
        unstable {
            script {
                echo "⚠️ Pipeline instable (tests échoués mais build réussi)"
            }
        }
    }
}
