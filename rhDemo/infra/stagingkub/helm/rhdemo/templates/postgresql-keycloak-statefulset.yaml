{{- if (index .Values "postgresql-keycloak").enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql-keycloak
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "postgresql-keycloak.labels" . | nindent 4 }}
    app: postgresql-keycloak
spec:
  serviceName: postgresql-keycloak
  replicas: 1
  selector:
    matchLabels:
      app: postgresql-keycloak
  template:
    metadata:
      labels:
        app: postgresql-keycloak
        {{- include "postgresql-keycloak.labels" . | nindent 8 }}
    spec:
      containers:
      - name: postgresql
        image: "{{ (index .Values "postgresql-keycloak").image.repository }}:{{ (index .Values "postgresql-keycloak").image.tag }}"
        imagePullPolicy: {{ (index .Values "postgresql-keycloak").image.pullPolicy }}
        ports:
        - name: postgresql
          containerPort: {{ (index .Values "postgresql-keycloak").service.port }}
          protocol: TCP
        env:
        - name: POSTGRES_DB
          value: {{ (index .Values "postgresql-keycloak").database.name }}
        - name: POSTGRES_USER
          value: {{ (index .Values "postgresql-keycloak").database.user }}
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ (index .Values "postgresql-keycloak").database.passwordSecret.name }}
              key: {{ (index .Values "postgresql-keycloak").database.passwordSecret.key }}
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgresql-data
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U {{ (index .Values "postgresql-keycloak").database.user }}
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - pg_isready -U {{ (index .Values "postgresql-keycloak").database.user }}
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        resources:
          {{- toYaml (index .Values "postgresql-keycloak").resources | nindent 10 }}
  volumeClaimTemplates:
  - metadata:
      name: postgresql-data
      labels:
        app: postgresql-keycloak
    spec:
      accessModes:
      - {{ (index .Values "postgresql-keycloak").persistence.accessMode }}
      {{- if (index .Values "postgresql-keycloak").persistence.storageClass }}
      storageClassName: {{ (index .Values "postgresql-keycloak").persistence.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ (index .Values "postgresql-keycloak").persistence.size }}
{{- end }}
