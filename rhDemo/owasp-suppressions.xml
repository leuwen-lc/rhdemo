<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!--
        CVE-2022-37620 — html-minifier-terser (faux positif de correspondance CPE)
        OWASP Dependency-Check associe html-minifier-terser (fork maintenu, utilisé par html-webpack-plugin)
        au CPE de kangax/html-minifier (projet original non maintenu, dont il dérive) faute de CPE dédié.
        La CVE elle-même (ReDoS via reCustomIgnore) concerne le code de html-minifier 4.0.0, pas le fork.
        Par ailleurs html-minifier-terser n'est atteint que via la chaîne de build
        @vue/cli-service (devDependency) -> html-webpack-plugin -> html-minifier-terser :
        jamais embarqué dans le bundle de production `vue-cli-service build`.
    -->
    <suppress>
        <notes>CVE-2022-37620 : faux positif CPE (html-minifier-terser confondu avec html-minifier, non maintenu) — de plus package uniquement présent dans la chaîne de build (devDependency @vue/cli-service), jamais dans le bundle de production.</notes>
        <packageUrl regex="true">^pkg:npm/html-minifier-terser@.*$</packageUrl>
        <cve>CVE-2022-37620</cve>
    </suppress>


    <!--
        GHSA-5m6q-g25r-mvwx — node-forge (DoS bnModInverse, aucun correctif publié)
        L'avis indique explicitement "Affected versions: All versions (including latest)" :
        aucune version corrigée n'existe à ce jour sur npm (montée de version 1.3.3 -> 1.4.0
        appliquée par npm audit fix pour les 3 autres avis node-forge, sans effet sur celui-ci).
        node-forge n'est atteint que via la chaîne de build
        @vue/cli-service (devDependency) -> @vue/cli-shared-utils -> @achrinza/node-ipc -> node-forge :
        jamais embarqué dans le bundle de production `vue-cli-service build`.
    -->
    <suppress>
        <notes>GHSA-5m6q-g25r-mvwx : aucun correctif node-forge disponible (avis officiel : toutes versions affectées) — package uniquement présent dans la chaîne de build (devDependency @vue/cli-service), jamais dans le bundle de production.</notes>
        <packageUrl regex="true">^pkg:npm/node-forge@.*$</packageUrl>
        <vulnerabilityName>GHSA-5m6q-g25r-mvwx</vulnerabilityName>
    </suppress>


    <!--
        GHSA-5c6j-r48x-rmvq — serialize-javascript (RCE, aucun correctif compatible)
        npm audit fix (package-lock-only) ne peut pas mettre à jour serialize-javascript :
        copy-webpack-plugin et css-minimizer-webpack-plugin (dépendances de @vue/cli-service)
        n'acceptent pas de version corrigée sans saut majeur (option force, exclue par la politique du skill).
        serialize-javascript n'est atteint que via ces plugins de build,
        jamais embarqué dans le bundle de production vue-cli-service build.
    -->
    <suppress>
        <notes>GHSA-5c6j-r48x-rmvq : pas de correctif compatible sans passage en force (copy-webpack-plugin/css-minimizer-webpack-plugin, dépendances de build @vue/cli-service) — jamais dans le bundle de production.</notes>
        <packageUrl regex="true">^pkg:npm/serialize-javascript@.*$</packageUrl>
        <vulnerabilityName>GHSA-5c6j-r48x-rmvq</vulnerabilityName>
    </suppress>


    <!--
        GHSA-w5hq-g745-h8pq — uuid (dépassement mémoire tampon, aucun correctif compatible)
        npm audit fix (package-lock-only) ne peut pas mettre à jour uuid :
        sockjs (dépendance de webpack-dev-server, lui-même dépendance de @vue/cli-service)
        n'accepte pas de version corrigée sans saut majeur (option force, exclue par la politique du skill).
        uuid n'est atteint que via cette chaîne de build (serveur de développement),
        jamais embarqué dans le bundle de production vue-cli-service build.
    -->
    <suppress>
        <notes>GHSA-w5hq-g745-h8pq : pas de correctif compatible sans passage en force (sockjs/webpack-dev-server, dépendances de build @vue/cli-service) — jamais dans le bundle de production.</notes>
        <packageUrl regex="true">^pkg:npm/uuid@.*$</packageUrl>
        <vulnerabilityName>GHSA-w5hq-g745-h8pq</vulnerabilityName>
    </suppress>


    <!--
        CVE-2026-42154 — micrometer-registry-prometheus et prometheus-metrics-core (faux positif)
        CVE-2026-42154 (CVSS 7.5 HIGH) : DoS par épuisement mémoire dans le endpoint /api/v1/read
        du SERVEUR Prometheus (binaire Go) — ne valide pas la taille déclarée dans les requêtes
        snappy-compressées avant d'allouer de la mémoire.
        FAUX POSITIF : les bibliothèques Java micrometer-registry-prometheus et prometheus-metrics-core
        sont des CLIENTS qui exportent des métriques VERS Prometheus. Elles n'implémentent pas
        l'endpoint /api/v1/read du serveur. OWASP Dependency-Check les associe incorrectement
        car elles partagent le mot "prometheus" dans leur nom de package.
        Fix officiel : serveur Prometheus 3.5.3 et 3.11.3 (sans rapport avec ces libs Java).
    -->
    <suppress>
        <notes>CVE-2026-42154 : faux positif — CVE concerne le serveur Prometheus Go (endpoint /api/v1/read), pas le client Java micrometer-registry-prometheus.</notes>
        <packageUrl regex="true">^pkg:maven/io\.micrometer/micrometer-registry-prometheus@.*$</packageUrl>
        <cve>CVE-2026-42154</cve>
    </suppress>

    <suppress>
        <notes>CVE-2026-42154 : faux positif — CVE concerne le serveur Prometheus Go (endpoint /api/v1/read), pas les clients Java prometheus-metrics-* (core, model, config, exposition-formats, etc.).</notes>
        <packageUrl regex="true">^pkg:maven/io\.prometheus/prometheus-metrics-[^@]+@.*$</packageUrl>
        <cve>CVE-2026-42154</cve>
    </suppress>


    <!--
        GHSA-mh99-v99m-4gvg (CVE-2026-14257) — brace-expansion (DoS OOM, aucun correctif compatible)
        brace-expansion <=5.0.7 permet un crash mémoire (uncatchable OOM) via expand() sur une entrée
        contenant de nombreux groupes d'accolades chaînés. Corrigé en 5.0.8, mais atteint ici en 1.1.17
        via minimatch:3.1.5 (frontend/package-lock.json?minimatch:3.1.5/brace-expansion:^1.1.7), lui-même
        dépendance transitive de la chaîne de build @vue/cli-service (glob/rimraf). npm audit fix
        (package-lock-only) ne peut pas mettre à jour brace-expansion sans saut majeur 1.x -> 5.x
        (option force, exclue par la politique du skill) : fixAvailable=false.
        brace-expansion n'est utilisé que par ces outils de build (résolution de patterns de fichiers),
        jamais embarqué dans le bundle de production vue-cli-service build.
    -->
    <suppress>
        <notes>GHSA-mh99-v99m-4gvg (CVE-2026-14257) : pas de correctif compatible sans passage en force (minimatch:3.1.5, dépendance de build @vue/cli-service) — jamais dans le bundle de production.</notes>
        <packageUrl regex="true">^pkg:npm/brace-expansion@.*$</packageUrl>
        <vulnerabilityName>GHSA-mh99-v99m-4gvg</vulnerabilityName>
    </suppress>


    <suppress>
        <notes>CVE-2026-45623 : postcss 7.0.39 (lecture de fichier arbitraire via sourceMappingURL, CVSS 9.1). Corrigé en 8.5.12, mais atteint ici en 7.0.39 exclusivement via node_modules/@vue/component-compiler-utils/node_modules/postcss (frontend/package-lock.json, contrainte semver postcss ^7.0.36 déclarée par @vue/component-compiler-utils:3.3.0, lui-même dépendance de build de @vue/cli-service, marqué dev=true dans package-lock.json — absent de dependencies/devDependencies directes de frontend/package.json). npm audit fix --package-lock-only rapporte fixAvailable=false : la mise à jour vers la branche 8.x impliquerait un saut de version majeure au-delà de la contrainte semver déclarée, ce qui nécessiterait l'option force, exclue par la politique du skill fixcve-auto. Cette instance de postcss ne traite que le CSS des composants .vue écrits par l'équipe (pipeline vue-cli-service build), jamais de CSS externe/non fiable comme le décrit le vecteur d'attaque de la CVE (CMS, upload utilisateur), et n'est jamais embarquée dans le bundle de production. Un second postcss (8.5.26, non affecté) coexiste au niveau supérieur du graphe de dépendances pour l'usage de production.</notes>
        <packageUrl regex="true">^pkg:npm/postcss@7\.0\.39$</packageUrl>
        <cve>CVE-2026-45623</cve>
    </suppress>

    <suppress>
        <notes>CVE-2026-69153 : postcss 7.0.39, même dépendance transitive que CVE-2026-45623 ci-dessus (via @vue/component-compiler-utils:3.3.0, devDependency de @vue/cli-service, jamais dans le bundle de production) — même justification, pas de correctif compatible sans passage en force.</notes>
        <packageUrl regex="true">^pkg:npm/postcss@7\.0\.39$</packageUrl>
        <cve>CVE-2026-69153</cve>
    </suppress>

    <suppress>
        <notes>CVE-2023-44270 : postcss 7.0.39, même dépendance transitive que CVE-2026-45623 ci-dessus (via @vue/component-compiler-utils:3.3.0, devDependency de @vue/cli-service, jamais dans le bundle de production) — même justification, pas de correctif compatible sans passage en force.</notes>
        <packageUrl regex="true">^pkg:npm/postcss@7\.0\.39$</packageUrl>
        <cve>CVE-2023-44270</cve>
    </suppress>

</suppressions>
